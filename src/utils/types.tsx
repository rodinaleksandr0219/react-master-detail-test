export interface PropTypes {
    title: string;
    body: string;
    publishedDate: number;
    level: number;
    url_action: Array<string>;
}

export interface OptionTypes {
    value: string;
    label: string;
};
